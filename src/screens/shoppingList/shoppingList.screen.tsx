import {Text, View} from 'react-native';
import styles from './styles';

const ShoppingListScreen = () => {
  return (
    <View style={styles.container}>
      <Text>
        Hola desde lista de compras
      </Text>
    </View>
  );
};

export default ShoppingListScreen;
